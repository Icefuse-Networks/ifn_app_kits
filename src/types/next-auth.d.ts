/**
 * NextAuth Type Extensions
 *
 * Types are provided by @icefuse/auth package.
 * This file is kept for backwards compatibility with existing imports.
 *
 * The @icefuse/auth package handles all type augmentations.
 */

// Re-export types from @icefuse/auth
export type {
  IcefuseUser,
  IcefuseSession,
  IcefuseJWT,
} from '@icefuse/auth'
